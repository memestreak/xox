# Source Directory (`src`)

This directory contains the core application logic and data.

## Structure

- [`app/`](./app/): Contains the React components, Audio Engine, and global styles.
- [`data/`](./app/data/): Contains static JSON data for drum kits and patterns. (Note: Data is currently nested under app for simplicity in this project's structure).
